import 'package:flutter/material.dart';
import 'package:homevio/Screens/Login/components/body.dart';

class LoginScreen extends StatelessWidget {
  static const idScreen = "login";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}